version '1.2.0'

intellij {
    // Uncomment the line below to test with the latest snapshot
    // version 'LATEST-EAP-SNAPSHOT'
    pluginName 'HasteIt'
}

dependencies {
    compile project(':ShareBase')
}

patchPluginXml {
    changeNotes """
      1.2.0: Switch to semantic versioning 
      """
}
