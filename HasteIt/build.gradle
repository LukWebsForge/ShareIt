version '1.15'

intellij {
    // Comment the line below to test with the latest snapshot
    // version 'LATEST-EAP-SNAPSHOT'
    pluginName 'HasteIt'
}

dependencies {
    compile project(':ShareBase')
}

patchPluginXml {
    changeNotes """
      1.15: Uses a new method of icon loading & the new notification system
      """
}
